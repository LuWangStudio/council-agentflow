jobs:
  - topic: api-service-design
    task: |
      Produce an API service execution plan that includes goals, scope, constraints,
      main steps, risks, and validation methods.
    status: pending
    # Optional: add this field manually during a human-review pause.
    # human_review: |
    #   Add a rollback strategy and clarify who owns each validation step.

  - topic: old-job
    task: |
      This job is already complete and should not run again.
    status: done
